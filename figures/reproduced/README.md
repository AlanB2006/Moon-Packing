# Reproduced figures

Run `moon-packing-figures` to populate this directory. Generated PDFs are
ignored by Git by default so that the repository stores source data and recipes
rather than duplicate products. The build delivered to the authors includes
example outputs for Figures 2-4 and 8-10.
